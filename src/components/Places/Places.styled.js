import styled from 'styled-components';

export const Wrapper = styled.div`
  position: relative;
`;

export const Input = styled.input`
  border-radius: 6px;
  margin-bottom: 30px;
  padding: 4px;
  font-size: 16px;
  font-weight: 500;
`;
